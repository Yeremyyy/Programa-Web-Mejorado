Create database Registros
use Registros

Create table Usuarios (
    id_usuario int auto_increment primary key,
    nombre varchar(90) not null,
    correo varchar(120) not null,
    Contraseña varchae(150) not null
);

